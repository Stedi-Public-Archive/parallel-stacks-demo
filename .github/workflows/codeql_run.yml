name: "Code Scanning - Action"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: "30 1 * * 0"
  workflow_dispatch: # Enables on-demand/manual triggering

jobs:
  CodeQL:
    uses: stedi/substrate-codeql/.github/workflows/codeql_run.yml@main
    secrets:
      ENG_GITHUB_TOKEN: ${{ secrets.ENG_GITHUB_TOKEN }}
